{
  "name": "omnitoken-enterprise",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild index.tsx --bundle --format=esm --outfile=index.js --external:react --external:react-dom --external:@google/genai --external:@supabase/supabase-js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}